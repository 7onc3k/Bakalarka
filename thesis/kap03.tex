\chapter{Metodika}
\label{kap:metodika}

%% ==========================================================================
%% 3.1 Návrh studie
%% ==========================================================================

\section{Výzkumný přístup}
\label{sec:vyzkumny-pristup}

\subsection{Volba výzkumné strategie}
\label{sec:volba-strategie}

Z~tří cílů formulovaných v~kapitole~\ref{kap:vymezeni} plynou
společné nároky na metodiku. Cíl~1, návrh sady metrik pokrývající
proces, kvalitu produktu a~zdroje, vyžaduje sadu metrik citlivou na
změny v~chování. To předpokládá porovnání více běhů s~různými
variantami instrukcí. Cíl~2, iterativní postup návrhu
instrukcí řízený těmito metrikami, vyžaduje navíc, aby běhy tvořily
sekvenci, v~níž lze sledovat vztah měření $\to$ diagnóza $\to$
úprava $\to$ další měření. Cíl~3, ablace složek instrukcí, stojí na
schopnosti porovnat běhy lišící se v~jediné složce. Všechny tři cíle
tedy společně kladou na metodiku dva požadavky. Prvním je opakované spouštění
agenta s~různými variantami instrukcí na témže projektu, druhým podrobná
analýza každého běhu napříč třemi dimenzemi (proces, produkt,
zdroje).

Tomuto profilu odpovídá případová studie, která v~empirickém
software engineering výzkumu slouží k~detailnímu zkoumání jevu
v~jeho reálném kontextu~\cite{runeson2009}. Řízený
experiment s~počtem běhů potřebným pro statistickou izolaci by byl
vzhledem k~ceně jedné iterace (tisíce tokenů, desítky minut)
nepraktický. Přednost proto dostává hloubka před šířkou, tedy jeden
projekt s~detailní analýzou každého běhu (git historie, transcript,
naměřené metriky).
\textcite{yin2018} pro tento typ výzkumu používá pojem
\textit{analytická generalizace}. Na jednom případě se ukazuje
princip, který lze dále ověřovat na dalších případech.
Praktický přínos této práce spočívá v~ověření, že navrženou sadu
metrik a~iterativní postup lze aplikovat na jednom případu, zatímco
analytická generalizace vymezuje způsob, jak opatrně číst
přenositelné principy.
Všechny běhy vycházejí ze stejné specifikace projektu, liší se
verzí instrukcí, výslednou git historií a~naměřenými hodnotami
metrik.

Touto volbou se práce vědomě staví mimo benchmark
paradigma~\cite{swebench2024}, které srovnává mnoho modelů na
mnoha úlohách s~agregovanými skóre. Cílem této studie není sestavit žebříček
modelů, ale podrobné porozumění tomu, jak iterativní úpravy
instrukcí mění chování jednoho agenta na jednom projektu. To je
úloha pro případovou studii, ne pro benchmark.

\paragraph{Pilotní fáze}
\phantomsection\label{sec:pilotni-iterace}%
Případová studie postupuje ve dvou fázích. Pilotní fáze slouží
k~nalezení použitelné sady instrukcí. První běh používá výchozí
\texttt{AGENTS.md}, tedy sadu instrukcí odvozenou z~literatury
o~instrukcích pro agentní vývoj. Další běhy už vznikají iterativně.
V~každém běhu se sleduje, zda agent dodržel požadovaný postup a~jak
kvalitní řešení vytvořil. Naměřené odchylky se potom používají jako
podklad pro další úpravu instrukcí. Pilotní fáze tak ukazuje, zda
metriky dávají dostatečně konkrétní zpětnou vazbu pro řízené
zlepšování instrukcí.

\paragraph{Ablace}
\phantomsection\label{sec:komparativni-variace}%
Ablační fáze vychází z~použitelné sady instrukcí nalezené v~pilotu.
Z~této sady se odebere vybraná složka a~sleduje se, zda se změní
chování agenta. Ablace tedy netvoří další ladicí cyklus, ale
kontrolované porovnání plné verze instrukcí s~její zjednodušenou
variantou. Každá ablace se provádí ve dvou nezávislých bězích se
stejným nastavením, aby bylo možné odlišit opakující se efekt změny
od přirozené variability nedeterministického modelu. Ani dva běhy
neposkytují statistickou průkaznost, a~výsledky proto mají
indikativní, ne kauzální povahu.

\subsection{Případ a~jednotky analýzy}
\label{sec:vyber-projektu}

\textit{Případem} v~Yinově smyslu~\cite{yin2018} je projekt
systému upomínek faktur. \textit{Jednotkami analýzy}
jsou jednotlivé běhy agenta nad tímto projektem (běhy z~pilotní fáze
a~z~ablačních variant), z~nichž každý poskytuje vlastní data pro
hodnocení napříč třemi dimenzemi (proces, produkt, zdroje).
Případová studie potřebuje projekt, na kterém lze opakovaně spouštět
agenta s~různými instrukcemi a~objektivně měřit výsledky. Projekt
musí mít deterministickou logiku (stejný vstup vždy dá stejný
výstup), aby bylo možné ověřit korektnost automatizovanými testy
a~mutation testingem. Zároveň musí být dostatečně malý pro více
experimentálních běhů, ale obsahovat reálné nuance (hraniční případy,
doménová pravidla), aby měl agent co řešit.

Zvolený případ představuje aplikaci pro automatické odesílání
připomínek k~nezaplaceným fakturám. Obsahuje stavový
automat (nová, po splatnosti, upomínaná, eskalovaná), časové výpočty
(pracovní dny, ochranné lhůty) a~pravidla pro eskalaci. Současně
jde o~úlohu dostatečně bohatou na netriviální rozhodování, ale stále
zvládnutelnou pro opakované běhy. Agent musí kombinovat stavové
přechody, časové výpočty a~testování hraničních případů, takže
metriky zachycují víc než průchod jednoduchou CRUD implementací.

\phantomsection\label{sec:konstrukce-specifikace}%
Specifikace projektu tvoří fixní zadání, které agent dostává
v~Issue~\#1. Issue má dvě části: \textbf{Requirements} (co business
potřebuje) a~\textbf{API Contract} (co musí implementace exportovat).
API kontrakt je současně jediným technickým omezením implementace
a~vstupem pro metriku~\ac{Q1}.
Plné znění specifikace je součástí doplňujících materiálů
odkazovaných v~příloze~\ref{app:doplnujici-materialy}.

\paragraph{Doménová logika}
Systém eskaluje neuhrazené faktury sérií stavů od přátelské upomínky
před splatností přes stupňující se urgentnost až po pozastavení
služby a~odpis pohledávky. Každá faktura má vlastní nezávislou
instanci, časové prodlevy se počítají v~pracovních dnech (víkendy
a~konfigurovatelné svátky se přeskakují automaticky v~aritmetice
timeoutů, nejsou samostatným stavem). Hlavní eskalační větev má 9
stavů spojených 8 časovými přechody (obrázek~\ref{fig:dunning-fsm}).
Popisky šipek používají notaci \texttt{[guard]\,/\,effect}. Guard
v~hranatých závorkách udává timeout v~pracovních dnech (\textit{bd}~=~business
days) od vstupu do předchozího stavu, effect za~lomítkem je akce
vrácená volajícímu (typicky odeslání e-mailu, u~přechodu do
\texttt{SUSPENDED} navíc pozastavení služby). Eskalační přechody
spouští událost \texttt{tick} při splnění guardu. Alternativně
\texttt{manual\_advance} posune stav na další bezpodmínečně (bez čekání
na timeout, v~diagramu nezakreslen). Vedle hlavní větve jsou
ze~všech aktivních stavů kdykoliv dostupné \texttt{PAID}
a~\texttt{CANCELLED} (z~9 stavů, vše kromě terminálů) a~\texttt{PAUSED}
(jen ze~6 aktivních: \texttt{OVERDUE} až \texttt{SUSPENDED}).

\begin{figure}[tbp]
\centering
\begin{tikzpicture}[
    >=stealth,
    s/.style={rectangle, draw, rounded corners=2pt,
              minimum height=0.55cm, minimum width=2.0cm,
              font=\scriptsize\sffamily, inner sep=2pt},
    term/.style={s, fill=black!8},
    side/.style={s, fill=blue!6},
    arr/.style={->, thick, gray!75},
    barr/.style={->, thick, blue!55!black},
    tx/.style={font=\scriptsize\sffamily, inner sep=1pt},
    note/.style={font=\scriptsize\sffamily\itshape, text=black!55, inner sep=1pt}
]
    %% --- Hlavní eskalace, vertikálně ---
    \node[s] (issued) {ISSUED};
    \node[s, below=0.5cm of issued]  (due)   {DUE\_SOON};
    \node[s, below=0.5cm of due]     (over)  {OVERDUE};
    \node[s, below=0.5cm of over]    (grace) {GRACE};
    \node[s, below=0.5cm of grace]   (rem1)  {REMINDER\_1};
    \node[s, below=0.5cm of rem1]    (rem2)  {REMINDER\_2};
    \node[s, below=0.5cm of rem2]    (final) {FINAL\_NOTICE};
    \node[s, below=0.5cm of final]   (susp)  {SUSPENDED};
    \node[term, below=0.5cm of susp] (wrof)  {WRITTEN\_OFF};

    %% --- Šipky s guards a~effects ---
    \draw[arr] (issued) -- node[tx, right]{[\,now\,$\geq$\,due\,$-7$\,bd\,] / email} (due);
    \draw[arr] (due)    -- node[tx, right]{[\,now\,$\geq$\,due\,]} (over);
    \draw[arr] (over)   -- node[tx, right]{[\,elapsed\,$\geq$\,3\,bd\,]} (grace);
    \draw[arr] (grace)  -- node[tx, right]{[\,elapsed\,$\geq$\,7\,bd\,] / email} (rem1);
    \draw[arr] (rem1)   -- node[tx, right]{[\,elapsed\,$\geq$\,14\,bd\,] / email} (rem2);
    \draw[arr] (rem2)   -- node[tx, right]{[\,elapsed\,$\geq$\,14\,bd\,] / email} (final);
    \draw[arr] (final)  -- node[tx, right]{[\,elapsed\,$\geq$\,7\,bd\,] / email\,+\,suspend\_service} (susp);
    \draw[arr] (susp)   -- node[tx, right]{[\,elapsed\,$\geq$\,30\,bd\,] / email} (wrof);

    %% --- Boční panel ---
    \node[term, anchor=west] (paid) at ($(issued.east)+(7.0cm,0)$)  {PAID};
    \node[term, below=0.4cm of paid]  (canc) {CANCELLED};
    \node[side, below=0.4cm of canc]  (paus) {PAUSED};

    \draw[arr] ($(paid.west)+(-1.4cm,0)$) -- node[tx, above]{payment} (paid.west);
    \draw[arr] ($(canc.west)+(-1.4cm,0)$) -- node[tx, above]{cancel}  (canc.west);
    \draw[arr] ($(paus.west)+(-1.4cm,0)$) -- node[tx, above]{paused}  (paus.west);

    \node[note, below=0.3cm of paus, text width=4.6cm, align=left] (resnote)
        {\textbf{resumed:} status\,$:=$\,pausedFrom; stateEnteredAt\,$:=$\,now\,$-$\,pausedElapsed};
    \draw[barr, dashed] (paus.south) -- (resnote.north);
\end{tikzpicture}
\caption[Stavový automat systému upomínek]{Stavový automat systému
upomínek faktur.}
\label{fig:dunning-fsm}
\end{figure}
\FloatBarrier

\paragraph{API kontrakt}
API kontrakt určuje, co musí agentova implementace poskytovat, aby ji
bylo možné porovnávat s~referenční sadou behavioral testů (\acs{Q2}).
Systém je čistá funkce \texttt{process(state, event, now)}, která
pro daný stav, událost a~aktuální čas vrátí nový stav a~seznam akcí.
Pevné jsou názvy 12 stavů (9 v~hlavní eskalační větvi a~3 boční:
\texttt{PAID}, \texttt{CANCELLED}, \texttt{PAUSED}), 6~typů událostí
(\texttt{tick}, \texttt{payment\_received}, \texttt{invoice\_cancelled},
\texttt{dunning\_paused}, \texttt{dunning\_resumed},
\texttt{manual\_advance}) a~3 typy akčních deskriptorů
(\texttt{send\_email}, \texttt{suspend\_service},
\texttt{resume\_service}). Stav nese kromě \texttt{status} pole pro
datum splatnosti, čas vstupu do aktuálního stavu, konfiguraci a~při
pauze \texttt{pausedFrom} s~\texttt{pausedElapsed}. Vnitřní
implementace (modulární rozdělení, funkční vs.\ objektový styl)
zůstává na agentovi. Kontrakt je úzký právě v~tom, co testy potřebují
volat a~pozorovat, a~všechno ostatní nechává otevřené.

\paragraph{Implicitní pravidla domény}
Stavový automat je sám o~sobě lineární a~implementačně přímočarý.
Obtížnost úlohy leží ve dvou implicitních pravidlech, která
specifikace klade na chování implementace. Jejich přehlédnutí vede
ke kódu, který může projít triviálními případy, ale selhává na
hraničních situacích. Za~prvé,
pause/resume musí přes pole \texttt{pausedElapsed} zachovat uplynulý
čas. Bez něj by se obnovení běhu muselo řešit posunem
\texttt{stateEnteredAt} zpět v~čase. Takové řešení funguje pro jednu
pauzu, ale selhává při opakovaném pause/resume, protože druhá pauza
ztratí historii první.
Za~druhé, všechny timeouty se počítají v~pracovních dnech, takže
\texttt{+14\,bd} od pátku není čtrnáct kalendářních dnů a~hranice
mezi víkendem a~pondělím jsou off-by-one zóna. Obě pravidla jsou
plně určená API kontraktem. Jejich zachycení ale vyžaduje doslovné
čtení typů a~doménového slovníku, ne pouze odvozování z~diagramu
automatu.

\paragraph{Acceptance criteria}
Specifikace obsahuje 25 acceptance criteria ve formátu Given/When/Then.
Pokrývají sedm okruhů: časové přechody eskalace, platby, terminální
stavy, storno, pauza/obnovení, manuální postup a~výpočet pracovních
dní. Ukázka jednoho kritéria z~okruhu pauza/obnovení:

\begin{quote}\small\sffamily
Given an invoice is in PAUSED state\\
When a \texttt{dunning\_resumed} event occurs\\
Then the state transitions back to the state it was in before pausing\\
And the timeout resumes from where it left off
\end{quote}

Formát Given/When/Then usnadňuje převod požadavků do referenčních
testů (\ac{Q2}) i~mapování acceptance criteria na agentovy vlastní
testy (\ac{Q4}). Specifikace obsahuje anglický doménový slovník
s~9~pojmy, například upomínkový proces (\textit{dunning}), ochrannou
lhůtu (\textit{grace period}), pracovní dny (\textit{business days})
a~popis akce (\textit{action descriptor}). Tyto pojmy sjednocují
terminologii mezi specifikací a~implementací.

\paragraph{Out of scope}
Specifikace explicitně vylučuje šest oblastí: opakování plateb,
úroky a~poplatky z~prodlení, částečné platby, odesílání e-mailů,
plánování (cron) a~persistenci dat. Sekce out of scope omezuje
riziko, že agent rozšíří řešení nad rámec sledovaného zadání,
a~drží úlohu na úrovni čisté business logiky.

\paragraph{Referenční testy jako oracle}
\phantomsection\label{sec:ref-implementace-postup}%
Pro metriku~\ac{Q2} (sec~\ref{sec:produktove-metriky}) byla souběžně
se specifikací připravena sada 42 referenčních testů metodou
spec-first TDD~\cite{mathews2024}: očekávané hodnoty pochází ze
specifikace, ne z~pozorování kódu. Testy jsou agentovi neviditelné
a~spouští se až po běhu samostatným skriptem, takže slouží jako
externí oracle, tedy zdroj očekávaných výsledků, který agent nemůže přepsat
(sec~\ref{sec:hodnoceni-ai-agentu}). Mutation testing nad referenční
testovací sadou dosáhl 91{,}9\,\%, což indikuje, že referenční testy samy
detekují cílené poruchy v~doménové logice. Tato hodnota slouží jako
pojistka kvality měřicího nástroje, ne jako srovnávací základna pro
agentovy běhy.

V~tomto designu vystupují tři roviny, které se snadno zaměňují.
\textit{Instrukce} v~souboru \texttt{AGENTS.md} jsou nezávislou
proměnnou, která se mezi běhy mění a~jejíž dopad se sleduje. \textit{Chování
agenta} (jak implementuje, jak strukturuje práci a~jaký kód vytváří)
je závislou proměnnou, která se měří. Systém upomínek faktur slouží
jako testovací prostředí, na~kterém se chování pozoruje. Není
předmětem zkoumání.

\subsection{Fixní proměnné}
\label{sec:fixni-promenne}

Aby bylo možné měřit vliv instrukcí, musí být všechno ostatní
co nejstabilnější. Hlavní měněnou proměnnou mezi běhy je obsah
souboru \texttt{AGENTS.md}, tedy procedurálních instrukcí, které
definují pracovní postup, omezení a~quality gates. Tento design
negarantuje dokonalou izolaci všech vlivů. Jeho cílem je omezit
zjevné vedlejší vstupy a~udělat jejich případný vliv auditovatelný.
Na rozdíl od generických context files,
které typicky popisují adresářovou strukturu a~build
příkazy~\cite{gloaguen2025agentsmd}, jde o~fokusované instrukce
s~konkrétními procedurami~\cite{skillsbench2025}.

Fixní proměnné jsou:

\textbf{Prázdné GitHub repo} obsahuje pouze \texttt{AGENTS.md}
a~konfiguraci agenta. Žádný existující kód, testy ani
\texttt{package.json}. Agent musí inicializovat projekt
a~zvolit strukturu sám.

\textbf{Specifikace projektu} (popsaná v~sekci~\ref{sec:konstrukce-specifikace})
zůstává napříč všemi běhy beze změny a~je dodávána agentovi
v~GitHub Issue~\#1 jako hlavní věcný vstup vedle instrukcí.

\textbf{Modely} jsou mezi běhy fixní. Agentní běhy provádí
MiniMax-M2.5, zvolený kvůli nízké ceně a~rychlosti při opakovaných
agentních bězích. Cílem práce je vyhodnotit sadu metrik a~iterativní
postup, ne srovnávat výkon modelů. Konkrétní agentní model proto
slouží jako součást experimentálního nastavení. Pro judge-based
metriky je fixně použit GLM-5 (Zhipu AI), záměrně z~jiné modelové
rodiny než agent. Volba vychází z~nízké ceny a~biasů LLM-as-judge
popsaných v~sekci~\ref{sec:llm-as-judge}.
Teplota modelu nebyla mezi běhy explicitně nastavena. Agent běžel
s~výchozí konfigurací poskytovatele.

\textbf{Běhové prostředí (Docker container)} zajišťuje, že agent
nemá přístup ke globální konfiguraci nástroje, MCP serverům ani
rozšířením nainstalovaným na hostitelském systému. Izolace tím
významně omezuje vliv vedlejších vstupů z~prostředí, ale
nevylučuje všechny zdroje variability. Sdílený
autentizační svazek zajišťuje konzistentní přístup k~API napříč
běhy.

\textbf{Harness OpenCode} je CLI nástroj, který orchestruje běh
agenta. Spravuje session, zprostředkovává volání modelu, zpřístupňuje
nástroje (čtení a~zápis souborů, shell, vyhledávání) a~zaznamenává
transcript. Harness sám není měřeným chováním. Měří se to, co agent
přes něj udělá. Verze nástroje je pinovaná v~Docker image, konfigurace
agenta (povolené nástroje, MCP servery) je součástí auditovatelného
balíku v~repozitáři práce. Volba OpenCode je dána open-source
licencí (umožňuje pinování verze a~záznam interní telemetrie),
strojově čitelným exportem session a~přístupem do interní databáze,
ze které čerpá metrika \acs{E3} (počet kompakcí kontextu).
Vlastnosti harness, které ovlivňují měření, jsou diskutovány
u~jednotlivých metrik, zejména tichý retry po selhání API
(sekce~\ref{sec:limity-vysledky}) a~automatická kompakce kontextu
(metrika \acs{E3}).

\textbf{System prompt agenta} (\texttt{build.md}) nahrazuje výchozí
system prompt nástroje OpenCode, jehož instrukce o~verzování
konfliktovaly s~procesními požadavky případové studie. Vlastní system prompt
obsahuje pouze obecné kódové konvence (styl, bezpečnost, zákaz
komentářů) a~odkaz na \texttt{AGENTS.md}. Veškeré procesní instrukce
jsou výhradně v~\texttt{AGENTS.md}, aby bylo možné změny procesního
chování přisuzovat této vrstvě obhajitelněji než při jejich
rozptýlení mezi více vstupů.

%% ==========================================================================
%% 3.2 Sada metrik
%% ==========================================================================

\section{Sada metrik}
\label{sec:sada-metrik}

Každý experimentální běh probíhá tak, že agent dostane prázdné
GitHub repo, specifikaci v~Issue~\#1 a~instrukce v~\texttt{AGENTS.md},
a~autonomně implementuje zadaný projekt. Výstup jednoho běhu (kód,
testy, git historie) se měří sadou 19~metrik. Proceduru jednoho běhu
(záznam, vyhodnocení a~diagnostickou úpravu instrukcí) popisuje
sekce~\ref{sec:procedura-behu}.

Sada metrik pokrývá tři otázky, tedy jak agent pracoval, co vyrobil
a~za jakou cenu. Tyto tři otázky odpovídají taxonomii proces /
produkt / zdroje popsané v~sekci~\ref{sec:mereni-kvality-obecne}.
Sada obsahuje 19~metrik ve třech kategoriích:

\begin{description}
    \item[P~-- proces (P1--P8)] Dodržel agent předepsaný postup?
          Měří se dodržování pravidel pracovního postupu z~instrukcí
          (\acs{P1}--\acs{P5}) a~kvalita procesních výstupů, jako jsou
          zprávy commitů, popisy issue a~popisy PR (\acs{P6}--\acs{P8}).
    \item[Q~-- kvalita produktu (Q1--Q8)] Je kód kvalitní?
          Měří se funkční korektnost (\acs{Q1}--\acs{Q2}), kvalita testů
          (\acs{Q3}--\acs{Q4}) a~udržovatelnost kódu (\acs{Q5}--\acs{Q8}).
    \item[E~-- zdroje (E1--E3)] Za jakou cenu?
          Zaznamenávají se spotřebované tokeny (\acs{E1}), čas (\acs{E2})
          a~počet kompakcí kontextu (\acs{E3}).
\end{description}

\paragraph{Tři osy klasifikace metriky}
Každá metrika je popsána třemi vlastnostmi. První je kategorie
\mgrp{P}, \mgrp{Q} nebo \mgrp{E}, tedy zda metrika sleduje proces,
produkt nebo zdroje. Druhá je způsob měření. Deterministické metriky
počítá skript, parser logu nebo statická analýza, zatímco
judge-based metriky hodnotí LLM-as-judge podle hodnoticího schématu. Třetí
vlastností je role v~hodnocení. Procesní a~produktové metriky mají
exit kritéria a~slouží k~posouzení, zda byl běh úspěšný. Metriky
zdrojů \acs{E1}--\acs{E3} exit kritéria nemají. Jejich hodnoty se
pouze zaznamenávají a~slouží jako kontext pro porovnání spotřeby
zdrojů a~stability kontextu během běhu.

\paragraph{LLM-as-judge protokol}
\phantomsection\label{sec:llm-as-judge-aplikace}%
Pět metrik (\acs{P6}, \acs{P7}, \acs{P8}, \acs{Q4}, \acs{Q8})
hodnotí vlastnosti, které nelze spolehlivě extrahovat
deterministickým skriptem. Proto používají LLM-as-judge s~modelem
GLM-5 (Zhipu AI). GLM-5 je z~jiné modelové rodiny než agentní
MiniMax-M2.5, protože modely mají tendenci zvýhodňovat výstupy svého
vlastního druhu, tedy vykazují self-preference bias~\cite{panickssery2024}.
Volba hodnotitele z~jiné rodiny tento bias snižuje~\cite{verga2024poll}.

Judge dostává výstupy běhu, například zdrojový kód, zprávy commitů,
popisy issue a~popisy PR. Hodnotí je podle pevného hodnoticího schématu
(\textit{scoring rubric}), které určuje posuzované dimenze a~význam
stupňů 1 až 3 (1 = nejnižší kvalita, 3 = nejvyšší kvalita). Stejný
prompt, hodnoticí schémata i~model GLM-5 se používají ve všech bězích,
aby bylo hodnocení mezi iteracemi srovnatelné.

Judge-based metriky mají v~této práci podpůrnou roli. Umožňují
strukturovaně zachytit vlastnosti, které deterministické nástroje
nepokrývají, ale nejsou interpretovány jako plně objektivní měření.

\subsection{Procesní metriky (P1--P8)}
\label{sec:procesni-metriky}

Procesní metriky měří \textit{jak} agent pracuje.
Instrukce v~\texttt{AGENTS.md} definují strukturovaný vývojový postup. Agent
má odvozovat práci ze specifikace, organizovat ji přes issues
a~větve, psát testy před implementací a~dokumentovat rozhodnutí
ve~zprávách commitů a~popisech PR.

Tato skupina metrik převádí procesní dimenzi z~teoretických
východisek do stop, které po sobě agent zanechává v~repozitáři
a~na GitHubu. Proto sleduje pořadí práce, vazbu mezi issues,
větvemi a~pull requesty, test-first postup a~kvalitu procesních
záznamů.

\subsubsection*{P1--P5: dodržení procesních pravidel (binární)}

Tabulka~\ref{tab:p1-p5} shrnuje deterministické procesní metriky.
Všechny mají binární výsledek splněno/nesplněno.

\begin{table}[htbp]
\centering
\caption{Procesní metriky dodržení pravidel P1--P5}
\label{tab:p1-p5}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\linewidth}{@{}lp{0.22\linewidth}Xp{0.16\linewidth}@{}}
\toprule
\textbf{Kód} & \textbf{Co měří} & \textbf{Jak se měří} & \textbf{Exit kritérium} \\
\midrule
\acs{P1} & Issues vznikla před kódem &
Porovnání \texttt{created\_at} nejstaršího issue vytvořeného agentem (GitHub API)
s~časem prvního commitu obsahujícího soubory v~\texttt{src/}
nebo \texttt{tests/}. & kontrola projde \\
\addlinespace
\acs{P2} & Branch per issue &
Počet vzdálených větví (bez \texttt{main}) vůči počtu issues, podmínka
\textit{branches $\geq$ issues} (žádné slučování více issues do jedné
větve). & kontrola projde \\
\addlinespace
\acs{P3} & Test-first &
Primárně se kontroluje, zda existuje commit s~prefixem \texttt{test:}
před commitem \texttt{feat:}. Jako doplňková kontrola se z~git historie
ověřuje, zda první změna v~\texttt{src/} nepředchází první změně
v~\texttt{tests/}. & kontrola projde \\
\addlinespace
\acs{P4} & PR linkován na issue &
Tělo každého PR (GitHub API) obsahuje regex \texttt{Closes \#N}. & kontrola projde \\
\addlinespace
\acs{P5} & Žádné modifikace existujících testů &
Metrika sleduje, zda agent měnil již existující testy v~adresáři
\texttt{tests/}. Kontrola používá \texttt{git diff -{}-diff-filter=M}.
Nově přidané testy se nezapočítávají, protože rozšiřování testovací sady
je v~zadání povolené. & kontrola projde \\
\bottomrule
\end{tabularx}
\end{table}
\FloatBarrier

\subsubsection*{P6--P8: kvalita procesních výstupů (LLM-as-judge)}

Tabulka~\ref{tab:p6-p8} shrnuje judge-based procesní metriky.

\begin{table}[htbp]
\centering
\caption{Judge-based procesní metriky P6--P8}
\label{tab:p6-p8}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\linewidth}{@{}lp{0.22\linewidth}Xp{0.16\linewidth}@{}}
\toprule
\textbf{Kód} & \textbf{Co hodnotí} & \textbf{Škála} & \textbf{Exit kritérium} \\
\midrule
\acs{P6} & Kvalita zpráv commitů &
1--3 (jeden logický záměr na commit, konvenční prefix, srozumitelnost co a~proč bylo změněno). & $\geq$ 2/3 \\
\addlinespace
\acs{P7} & Kvalita popisů issue &
1--3 (jasnost scope, přítomnost acceptance criteria, dostatečnost pro
implementaci). & $\geq$ 2/3 \\
\addlinespace
\acs{P8} & Kvalita popisů PR &
1--3 (odkaz na issue, popis co a~proč, dostatečnost pro code review). & $\geq$ 2/3 \\
\bottomrule
\end{tabularx}
\end{table}
\FloatBarrier

\subsection{Produktové metriky (Q1--Q8)}
\label{sec:produktove-metriky}

Produktové metriky měří \textit{co} agent vyrobil. Z~osmi
charakteristik ISO/IEC~25010 (sekce~\ref{sec:dimenze-kvality})
tato sada sleduje dvě: \textit{funkční vhodnost}
(\acs{Q1}--\acs{Q4}: agent vytváří správné řešení)
a~\textit{udržovatelnost} (\acs{Q5}--\acs{Q8}: agent vytváří kód
vhodný k~dalšímu pochopení a~rozvoji). Zbylé charakteristiky
ISO/IEC~25010 by vyžadovaly jiný typ úlohy. Bezpečnost předpokládá
autentizaci, oprávnění nebo práci s~citlivými daty, výkonnost
měřitelnou zátěž a~použitelnost uživatelské rozhraní. Zvolený projekt
je záměrně omezen na doménovou logiku, takže sada Q~metrik sleduje
funkční korektnost, kvalitu testů a~udržovatelnost.

\subsubsection*{Funkční korektnost (Q1--Q2)}

Tabulka~\ref{tab:q1-q2} shrnuje metriky funkční korektnosti.

\begin{table}[htbp]
\centering
\caption{Metriky funkční korektnosti Q1--Q2}
\label{tab:q1-q2}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\linewidth}{@{}lp{0.22\linewidth}Xp{0.16\linewidth}@{}}
\toprule
\textbf{Kód} & \textbf{Co měří} & \textbf{Jak se měří} & \textbf{Exit kritérium} \\
\midrule
\acs{Q1} & Kompatibilita s~API kontraktem &
Referenční typy se importují a~zkompilují proti agentovu kódu
(\texttt{tsc}). Výsledek je binární podle toho, zda typy odpovídají
kontraktu. & match \\
\addlinespace
\acs{Q2} & Úspěšnost referenčních testů &
Referenční testy se spouštějí nástrojem Vitest proti agentovu kódu.
Výsledek je počet úspěšných testů z~42. & 42/42 \\
\bottomrule
\end{tabularx}
\end{table}
\FloatBarrier

\acs{Q1} ověřuje, zda implementace odpovídá API kontraktu. Pokud
implementace API kontrakt nesplňuje, referenční testy \acs{Q2} se
nedají spustit. Splnění \acs{Q1} ale samo neznamená, že se implementace
chová správně. To ověřují až referenční testy \acs{Q2}.

\phantomsection\label{sec:referencni-testy}%
Případová studie rozlišuje dva druhy testů. \textit{Referenční testy}
(měřené metrikou \acs{Q2}) jsou odvozené ze specifikace metodou
TDD~\cite{mathews2024} a~spouštěné po běhu samostatným skriptem,
takže pro agenta jsou neviditelné. \textit{Agentovy vlastní testy}
píše agent během běhu a~jsou viditelné i~spustitelné agentem. Vztahuje
se k~nim metrika \acs{P5}. Referenční testy slouží jako externí
oracle, tedy zdroj očekávaných výsledků, který agent nemůže přepsat
(sekce~\ref{sec:hodnoceni-ai-agentu}).

\subsubsection*{Kvalita testů (Q3--Q4)}

Tabulka~\ref{tab:q3-q4} shrnuje metriky kvality testů.

\begin{table}[htbp]
\centering
\caption{Metriky kvality testů Q3--Q4}
\label{tab:q3-q4}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\linewidth}{@{}lp{0.22\linewidth}Xp{0.16\linewidth}@{}}
\toprule
\textbf{Kód} & \textbf{Co měří} & \textbf{Jak se měří} & \textbf{Exit kritérium} \\
\midrule
\acs{Q3} & Schopnost agentových testů detekovat chyby &
Nástroj Stryker spouští mutation testing nad agentovými testy
a~zdrojovým kódem. Výsledkem je mutation score, tedy podíl mutovaných
variant programu, které testy odhalí. & $\geq$ 80\,\% \\
\addlinespace
\acs{Q4} & Pokrytí acceptance criteria agentovými testy\protect\footnotemark &
Specifikace obsahuje 25~acceptance criteria. Hodnocení určí, kolik
z~nich má odpovídající test v~agentově testovací sadě. Výsledek má tvar
\texttt{N/25}. & 25/25 \\
\bottomrule
\end{tabularx}
\end{table}
\footnotetext{Na rozdíl od~\acs{Q2}, která ověřuje chování výsledné
implementace, \acs{Q4} sleduje, zda agentovy vlastní testy pokrývají
požadovaná acceptance criteria.}
\FloatBarrier

\subsubsection*{Kvalita kódu (Q5--Q8)}

Tabulka~\ref{tab:q5-q8} shrnuje metriky kvality kódu.

\begin{table}[htbp]
\centering
\caption{Metriky kvality kódu Q5--Q8}
\label{tab:q5-q8}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\linewidth}{@{}lp{0.22\linewidth}Xp{0.16\linewidth}@{}}
\toprule
\textbf{Kód} & \textbf{Co měří} & \textbf{Jak se měří} & \textbf{Exit kritérium} \\
\midrule
\acs{Q5} & Lint warnings + errors &
ESLint s~fixní konfigurací (recommended + strict TypeScript rules).
Konfigurace je součástí experimentální infrastruktury, ne agentova repo,
takže ji agent nemůže měnit. Výstupem je celkový počet varování a~chyb linteru. & 0 \\
\addlinespace
\acs{Q6} & Typové chyby &
\texttt{tsc -{}-noEmit} ve strict mode. Doplňuje počet explicitních
\texttt{any} ve zdrojových souborech (grep v~\texttt{src/**/*.ts}) jako
proxy pro obcházení typového systému. & 0 \\
\addlinespace
\acs{Q7} & Porušení složitosti &
ESLint \texttt{complexity} rule reportuje funkce s~cyklomatickou
složitostí nad limitem 10. Výstupem je počet porušení tohoto limitu. & 0 \\
\addlinespace
\acs{Q8} & Designová kvalita kódu &
Hodnocení posuzuje pojmenování, oddělení zodpovědností, idiomatický
TypeScript, kvalitu dokumentace a~zbytečnou komplexitu. Celkové skóre
je minimum dílčích dimenzí, aby silná dimenze nemaskovala slabou. & $\geq$ 2/3 \\
\bottomrule
\end{tabularx}
\end{table}
\FloatBarrier

\subsection{Metriky zdrojů (E1--E3)}
\label{sec:metriky-zdroju}

Metriky zdrojů zachycují, kolik běh agenta spotřeboval tokenů a~času
a~zda během něj došlo ke kompakci kontextu. Na rozdíl od procesních
a~produktových metrik nemají \acs{E1}--\acs{E3} exit kritérium. Jejich
hodnoty slouží k~porovnání spotřeby zdrojů a~stability kontextu během běhu
mezi iteracemi.

Tato skupina metrik převádí zdrojovou dimenzi z~teoretických
východisek do agentního prostředí. Lidské úsilí zde není přímo
pozorovaným vstupem, proto se sledují dostupné provozní ukazatele.
Jde o~spotřebované tokeny, reálný čas běhu a~kompakce kontextového
okna.

\begin{table}[htbp]
\centering
\caption{Metriky zdrojů E1--E3}
\label{tab:e1-e3}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\linewidth}{@{}lMX@{}}
\toprule
\textbf{Kód} & \textbf{Co měří} & \textbf{Jak se měří} \\
\midrule
\acs{E1} & Spotřeba tokenů &
Tři složky z~exportu session v~tisících: maximální vstup na jednom
kroku, součet výstupních tokenů a~součet cache tokenů. \\
\addlinespace
\acs{E2} & Trvání běhu &
Reálný čas běhu v~minutách od spuštění agenta po poslední commit. \\
\addlinespace
\acs{E3} & Počet kompakcí &
Počet kompakcí kontextu během běhu, čtený přímo z~OpenCode
(\texttt{time\_compacting}). Kompakce indikuje, že agent
vyčerpal kontextové okno a~prostředí muselo obsah zhustit.
Metrika nemá exit kritérium. \\
\bottomrule
\end{tabularx}
\end{table}
\FloatBarrier

%% ==========================================================================
%% 3.3 Procedura jedné iterace
%% ==========================================================================

\section{Procedura jedné iterace}
\label{sec:procedura-behu}

Sekce~\ref{sec:pilotni-iterace} vymezila rozdíl mezi pilotní
a~ablační fází. Tato část popisuje operační kroky, které se provedou
u~jednoho běhu agenta. V~pilotní fázi na sebe navazují čtyři kroky:
spuštění běhu, měření, diagnóza a~úprava instrukcí pro další běh
(obr.~\ref{fig:iterativni-cyklus}). Pokud další běh vede k~regresi,
navazující úprava vychází z~poslední úspěšné verze instrukcí.
V~ablační fázi se používá stejné spuštění a~měření, ale bez průběžné
úpravy téže varianty instrukcí.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=1.8cm,
    box/.style={rectangle, draw, rounded corners=3pt,
                minimum width=2.2cm, minimum height=0.8cm,
                font=\small\sffamily},
    arr/.style={->, thick}
]
    \node[box] (run)      {Spuštění};
    \node[box, right=of run]     (measure) {Měření};
    \node[box, right=of measure] (diagnose){Diagnóza};
    \node[box, right=of diagnose](fix)     {Úprava};

    \draw[arr] (run)      -- (measure);
    \draw[arr] (measure)  -- (diagnose);
    \draw[arr] (diagnose) -- (fix);
    \draw[arr] (fix.south) -- ++(0,-0.5) -| (run.south)
        node[midway, below, font=\footnotesize\sffamily]
        {opakovat podle diagnostiky};
\end{tikzpicture}
\caption{Iterativní cyklus pilotní fáze.}
\label{fig:iterativni-cyklus}
\end{figure}
\FloatBarrier

\subsection{Spuštění, záznam a~vyhodnocení běhu}
\label{sec:zaznam-bezhu}

Každý běh zakládá samostatné experimentální prostředí pro jednu
verzi instrukcí a~produkuje strukturovaný balík záznamů, který
umožňuje zpětně rekonstruovat naměřené hodnoty i~chování agenta. Pro
orientaci v~následujícím popisu ukazuje
výpis~\ref{lst:experiment-materials} zjednodušenou strukturu
relevantních částí repozitáře.

\begin{lstlisting}[
basicstyle=\footnotesize\ttfamily,
frame=single,
rulecolor=\color{gray!50},
backgroundcolor=\color{gray!5},
xleftmargin=0.8em,
xrightmargin=0.8em,
framesep=5pt,
abovecaptionskip=4pt,
belowcaptionskip=8pt,
aboveskip=8pt,
belowskip=10pt,
breaklines=true,
breakindent=0pt,
columns=fullflexible,
keepspaces=true,
literate={č}{{\v{c}}}1{š}{{\v{s}}}1{ř}{{\v{r}}}1{ž}{{\v{z}}}1{ý}{{\'{y}}}1{á}{{\'{a}}}1{í}{{\'{i}}}1{é}{{\'{e}}}1{ě}{{\v{e}}}1{ú}{{\'{u}}}1{ů}{{\r{u}}}1{ó}{{\'{o}}}1{ť}{{\v{t}}}1{ď}{{\v{d}}}1{ň}{{\v{n}}}1,
caption={Struktura experimentálních materiálů},
label={lst:experiment-materials}
]
experiments/                         # měřicí infrastruktura, běhy a reference
  infra/                             # měřicí infrastruktura a vstupy
    inputs/                          # build.md, AGENTS.md a specifikace zadání
    scripts/ts/                      # skripty pro spuštění a vyhodnocení
      new-run.ts                     # založení a spuštění běhu
      analyze-run.ts                 # extrakce deterministických metrik
      judge.ts                       # LLM-as-judge hodnocení
    judge/                           # hodnoticí schémata
  runs/                              # výstupy jednotlivých běhů
    pilot-rN/
      transcript.json                # úplný záznam session
      FINDINGS.md                    # naměřené hodnoty a popis chování
  reference/                         # referenční implementace a testy
\end{lstlisting}

\textbf{Spuštění.} \texttt{new-run.ts} vytvoří pro daný běh lokální adresář
\texttt{experiments/runs/runName/} a~soukromý GitHub repozitář
\texttt{bp-runName}. Do adresáře zkopíruje zvolenou verzi
\texttt{AGENTS.md}, fixní system prompt \texttt{build.md}, konfiguraci
OpenCode a~automatizační plugin. Po inicializačním commitu vytvoří
v~GitHubu Issue~\#1 se specifikací projektu a~spustí agenta v~Docker
containeru. Agent pracuje v~adresáři běhu jako ve svém pracovním prostoru
a~dostává úkol implementovat Issue~\#1 podle \texttt{AGENTS.md}. Po
ukončení běhu skript uloží identifikátor session, exit code a~export
session do \texttt{transcript.json}, který obsahuje úplný záznam
tool calls, vstupů a~výstupů agenta.

\textbf{Sběr metrik.} Po skončení běhu se spouštějí dva vyhodnocovací
skripty. \texttt{analyze-run.ts} extrahuje deterministické metriky z~git
historie, GitHub API a~transcriptu (\acs{P1}--\acs{P5},
\acs{Q1}--\acs{Q3}, \acs{Q5}--\acs{Q7}, \acs{E1}--\acs{E3}).
Skript \texttt{judge.ts} spouští LLM-as-judge (GLM-5) pro
\mmet{qual} (\acs{P6}--\acs{P8}, \acs{Q4}, \acs{Q8})
podle hodnoticích schémat uložených v~repozitáři práce. Souhrnným výstupem
je \texttt{FINDINGS.md} s~tabulkou metrik a~faktickým popisem
chování agenta.

\textbf{Auditovatelný balík.} Každý běh má vlastní adresář
\texttt{experiments/runs/pilot-rN/} obsahující transcript,
naměřené metriky a~\texttt{FINDINGS.md}. K~iteraci jako celku
patří navíc \texttt{DIAGNOSIS.md} s~analytickým výstupem
(sekce~\ref{sec:diagnostika-uprava}) a~záznam v~changelogu
zdůvodňující úpravu \texttt{AGENTS.md} pro další běh.
Tabulka~\ref{tab:zaznamy-bezhu} shrnuje strukturu balíku.

\begin{table}[htbp]
\centering
\caption{Záznamy pořízené pro jeden běh a~jednu iteraci}
\label{tab:zaznamy-bezhu}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\linewidth}{@{}lX@{}}
\toprule
\textbf{Soubor} & \textbf{Obsah} \\
\midrule
\texttt{transcript.json}            & úplný záznam session (tool calls, vstup, výstup) \\
\addlinespace
\texttt{FINDINGS.md}                & tabulka metrik a~faktický popis chování \\
\addlinespace
\texttt{DIAGNOSIS.md}               & klasifikace příčin a~návrh změn \\
\addlinespace
\texttt{changelog/r$N$-r$N{+}1$.md} & zdůvodnění úprav \texttt{AGENTS.md} (bylo / je / pozorování / diagnóza / odůvodnění) \\
\bottomrule
\end{tabularx}
\end{table}
\FloatBarrier

Doplňující materiály včetně operační procedury, šablon
\texttt{FINDINGS.md} a~\texttt{DIAGNOSIS.md}, verzí
\texttt{AGENTS.md}, hodnoticích schémat, transcriptů session,
surových výstupů nástrojů a~plného znění specifikace jsou uloženy
v~repozitáři práce. Přímé odkazy na stav zafixovaný tagem
\texttt{thesis-final} uvádí
příloha~\ref{app:doplnujici-materialy}.


\subsection{Diagnostika a~úprava instrukcí}
\label{sec:diagnostika-uprava}

Diagnostika převádí naměřené odchylky na vysvětlení, proč agent
selhal nebo odbočil od očekávaného postupu. Z~diagnózy potom vychází
konkrétní úprava \texttt{AGENTS.md}. Vstupem je
\texttt{FINDINGS.md} z~předchozího běhu
(sekce~\ref{sec:zaznam-bezhu}), výstupem \texttt{DIAGNOSIS.md},
upravená \texttt{AGENTS.md} pro příští běh a~záznam v~changelogu.

\textbf{Diagnostické rámce.} Diagnóza identifikuje, kde a~proč
agent nedodržel očekávané chování. Opírá se o~pět rámců, z~nichž
každý klade jinou diagnostickou otázku
(tabulka~\ref{tab:diagnostika-ramce}).

\begin{table}[htbp]
\centering
\caption{Diagnostické rámce a~otázky, které kladou}
\label{tab:diagnostika-ramce}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\linewidth}{@{}lX@{}}
\toprule
\textbf{Rámec} & \textbf{Diagnostická otázka} \\
\midrule
\textcite{mao2025fse}                          & Jsou komponenty instrukce přítomné a~ve~správném pořadí? \\
\textcite{hassan2025sase}                      & Vyvažuje obsah cíl (briefing), postup (loop) a~omezení (mentor)? \\
\textcite{razavi2025}, \textcite{breunig2025}  & Proč agent instrukci opakovaně ignoruje a~jak ji přestrukturovat? \\
\textcite{lulla2026}                           & Které typy obsahu reálně přispívají k~chování?\,$^{*}$ \\
Filter \textcite{skillsbench2025}              & Kdyby tento řádek chyběl, udělal by agent neočividnou chybu? \\
\bottomrule
\end{tabularx}

\vspace{0.3em}
{\footnotesize $^{*}$ Lulla měří přítomnost celého souboru, ne jednotlivé
typy obsahu, proto se rámec používá interpretativně.}
\end{table}
\FloatBarrier

\textbf{Postup diagnózy.} Z~tabulky metrik se vyberou nesplněné
nebo hraniční hodnoty, dohledají se faktické projevy ve
\texttt{FINDINGS.md} a~v~artefaktech (git log, issues, PR,
transcript), aby bylo možné odlišit jednorázové selhání běhu od
opakujícího se problému instrukcí. Identifikovaná příčina se
klasifikuje podle tabulky~\ref{tab:diagnostika-mapa}, která mapuje
typ příčiny na primární rámec a~typ opravy. Tabulka sjednocuje zápis
diagnózy napříč iteracemi.

\begin{table}[htbp]
\centering
\caption{Mapování klasifikace příčiny na diagnostický rámec a~typ opravy}
\label{tab:diagnostika-mapa}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular*}{\linewidth}{@{\extracolsep{\fill}}lll@{}}
\toprule
\textbf{Klasifikace příčiny} & \textbf{Primární rámec} & \textbf{Typ opravy} \\
\midrule
Chybějící / příliš obecná složka  & Mao             & Doplnění / zpřesnění \\
Nevyvážený obsah                  & Hassan          & Přestrukturování \\
Opakovaně ignorovaná instrukce    & Razavi, Breunig & Přestrukturování + verifikace \\
Redundantní obsah                 & Lulla + filter Li  & Odebrání \\
\bottomrule
\end{tabular*}
\end{table}
\FloatBarrier

\textbf{Úprava.} Na základě diagnózy se upraví \texttt{AGENTS.md}.
Každá změna odpovídá jednomu identifikovanému problému a~je
zaznamenaná v~changelogu ve formátu \textit{bylo / je / pozorování /
diagnóza / odůvodnění}. Záznam umožňuje zpětně dohledat, proč daná
úprava vznikla a~co měla opravit. Při úpravách má přednost zpřesnění
nebo přestrukturování existující instrukce před pouhým přidáváním
dalšího textu.

Úpravy instrukcí se v~této studii neprovádějí automatickou
optimalizací promptu. Ruční diagnostický postup je zvolen proto, aby
každá změna měla dohledatelnou vazbu na naměřenou odchylku
a~konkrétní problém v~chování agenta.


%% ==========================================================================
%% 3.4 Přehled metrik
%% ==========================================================================

\section{Přehledová tabulka}
\label{sec:prehledova-tabulka}

Tabulka~\ref{tab:metriky-prehled} shrnuje všechny metriky na jednom
místě. Sběr se liší podle typu metriky. U~\mmet{det} sloupec
\textit{Nástroj} uvádí konkrétní automatizovaný nástroj, zatímco
u~\mmet{qual} uvádí LLM-as-judge. Sloupec
\textit{Exit kritérium} uvádí podmínku úspěchu pro \mgrp{P}
a~\mgrp{Q}. Deskriptivní indikátory bez prahu, tedy celá zdrojová
skupina E1--E3, mají v~tomto sloupci pomlčku a~slouží jen
k~porovnání mezi běhy.

\begin{table}[htbp]
\centering
\caption{Sada metrik podle kódu, měřené vlastnosti, nástroje a~exit kritéria}
\label{tab:metriky-prehled}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\linewidth}{@{}p{0.06\linewidth}Xp{0.30\linewidth}p{0.18\linewidth}@{}}
\toprule
\textbf{Kód} & \textbf{Metrika} & \textbf{Nástroj} & \textbf{Exit kritérium} \\
\midrule
\multicolumn{4}{@{}l}{\textit{Procesní (P): jak agent pracuje}} \\[2pt]
P1 & Issues before code            & git, GitHub API & kontrola projde \\
P2 & Branch per issue              & git, GitHub API & kontrola projde \\
P3 & Test-first commits            & git log         & kontrola projde \\
P4 & PRs linked to issues          & GitHub API      & kontrola projde \\
P5 & No existing test modifications & git diff       & kontrola projde \\
P6 & Kvalita zpráv commitů         & LLM-as-judge (GLM-5) & $\geq$ 2/3 \\
P7 & Kvalita popisů issue          & LLM-as-judge (GLM-5) & $\geq$ 2/3 \\
P8 & Kvalita popisů PR             & LLM-as-judge (GLM-5) & $\geq$ 2/3 \\
\midrule
\multicolumn{4}{@{}l}{\textit{Produktové (Q): co agent vyrobil}} \\[2pt]
Q1 & API contract match         & tsc (import + typecheck) & match \\
Q2 & Úspěšnost ref.\ testů      & Vitest (42 testů)       & 42/42 \\
Q3 & Mutation score              & Stryker                 & $\geq$ 80\,\% \\
Q4 & AC coverage agentových testů (25 krit.) & LLM-as-judge (GLM-5)   & 25/25 \\
Q5 & Lint warnings               & ESLint                  & 0 \\
Q6 & Typecheck errors            & tsc \texttt{-{}-noEmit} & 0 \\
Q7 & Porušení složitosti         & ESLint (complexity)     & 0 \\
Q8 & Design quality              & LLM-as-judge (GLM-5)   & $\geq$ 2/3 \\
\midrule
\multicolumn{4}{@{}l}{\textit{Zdroje (E): za jakou cenu}} \\[2pt]
E1 & Vstup / výstup / $\Sigma$ cache (tis.) z exportu & OpenCode export & --- \\
E2 & Trvání (minuty)             & session timestamps & --- \\
E3 & Počet kompakcí kontextu & OpenCode (\texttt{time\_compacting}) & --- \\
\bottomrule
\end{tabularx}
\end{table}
\FloatBarrier

%% ==========================================================================
%% 3.5 Omezení a validita
%% ==========================================================================

\section{Omezení a~validita}
\label{sec:omezeni-validita}

Případová studie nese omezení daná zvolenou metodikou. Jejich dopad
lze popsat přes konstruktovou, interní a~externí validitu podle rámce
pro případové studie~\cite{runeson2009}.

\textbf{Konstruktová validita.}
Sada metrik je autorskou kombinací procesních, produktových
a~zdrojových ukazatelů. Nejde o~ověřený standard pro hodnocení
agentů, ale o~návrh operacionalizovaný pro tuto případovou studii.
Část metrik (\acs{P6}, \acs{P7}, \acs{P8}, \acs{Q4}, \acs{Q8})
navíc závisí na LLM-as-judge hodnocení. Tyto metriky zachycují
vlastnosti, které nelze spolehlivě extrahovat deterministickým
skriptem, ale jejich spolehlivost nebyla validována proti lidským
hodnotitelům. Proto jsou interpretovány jako podpůrné judge-based
indikátory. Hlavní oporu vyhodnocení tvoří deterministické metriky
a~auditovatelné artefakty.

\textbf{Interní validita.}
Sílu závěrů omezuje nedeterminismus LLM. Stejné instrukce mohou
v~různých bězích vést k~odlišnému chování agenta~\cite{razavi2025}.
Pilotní běhy proto slouží k~postupnému ladění instrukcí, ne
k~samostatnému dokazování účinku každé jednotlivé změny. Ablační fáze toto omezení
zmírňuje dvěma nezávislými běhy pro každou variantu, ale ani tak
neposkytuje statistickou sílu. Závěry z~ablací proto mají
indikativní povahu. Dalším omezením je role autora při diagnostice
a~úpravě instrukcí. Riziko jednostranné interpretace snižuje pevný
diagnostický postup, changelog změn, oddělení měřicí infrastruktury
od agentova prostředí a~převaha deterministických metrik tam, kde je
to možné.

\textbf{Externí validita.}
Případová studie na jednom projektu, jednom modelu a~jednom agentním
nástroji neumožňuje statistickou generalizaci. Ve smyslu analytické
generalizace~\cite{yin2018} může jeden případ ukázat principy
postupu, ne obecné zákonitosti pro všechny agenty a~projekty.
Zvolený projekt má deterministickou doménovou logiku a~nemusí
reprezentovat úlohy s~uživatelským rozhraním, strojovým učením nebo
nedeterministickými výstupy. Z~těchto podmínek lze zobecnit hlavně
způsob práce s~metrikami, tedy jak běh zaznamenat, vyhodnotit
a~použít výsledky k~úpravě instrukcí. Nelze z~nich odvodit, že stejné
hodnoty metrik nebo stejné instrukce budou fungovat u~jiného modelu,
nástroje nebo projektu.

Výzkum nezahrnuje lidské účastníky. Data (git log, metriky, session
transcripts), zdrojový kód i~experimentální artefakty jsou veřejně
dostupné.


%% ==========================================================================
%% Audit trail, starý obsah
%% ==========================================================================
